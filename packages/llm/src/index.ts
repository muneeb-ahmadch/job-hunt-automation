export const llmScaffold = {
  packageName: "@job-hunt/llm",
  purpose: "OpenAI Responses API wrapper, schema validation, prompt execution, caching, and retries",
  requiredPaidDependency: "OpenAI API only"
} as const;
